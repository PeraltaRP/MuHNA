name: muhna
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^2.1.0
  flutter_barcode_scanner: ^2.0.0
  flutter_launcher_icons: "^0.9.2"
  cupertino_icons: ^1.0.2
  url_launcher: ^6.1.4
  geolocator: ^8.2.1
  geocoding: ^2.0.4
  page_transition: ^2.0.9
  provider: ^5.0.0
  http: ^0.13.4
  shared_preferences: ^2.0.15
  gsheets: ^0.4.2
  intl: ^0.17.0
  woosignal: ^3.0.5



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/cidades/cidades.json
 
flutter_icons:
  android: true
  ios: true
  # image_path: "assets/icon/icon2.png"
  image_path: "assets/icon/icon3.png"

  # comando para trocar o icone:
  #  flutter pub run flutter_launcher_icons:main